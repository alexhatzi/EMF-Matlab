clc
clear

syms t k z 

s = 0 ; 
u = 4*pi*10^(-7) ; 
e =  4 * 8.854*10^(-12) ; 


E = 20*sin(  (10^(8))*t - k*z) ;

% Equation 7.18 
% K = ω * sqrt(μ*ε)

K = (10^(8)) * sqrt(u*e) 

Efield = [0 E 0] ;
Field = [diff(x) (y) (z)] 
H = curl(Efield,Field)